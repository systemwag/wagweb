-- Категории записей в таблице projects (реестр СМР).
--
-- Контекст: projects = СМР/строительство (см. комментарий в PrintBrochure.tsx —
-- projects → /projects, maintenance_projects → /maintenance, design_projects → /design).
-- Три записи стояли с категорией «Проектирование», то есть описывали проектные
-- работы внутри СМР-реестра. По объектам Aktobe Steklo и QazCement выполнены
-- ОБА вида работ: проектирование (закрыто, положительные заключения госэкспертизы —
-- кейсы 02.1–02.3 и 03 брошюры) и СМР (в работе). Проектная сторона заводится
-- отдельными записями в design_projects (миграция 20260726055414), а эти строки
-- остаются СМР — им возвращается строительная категория.
--
-- Категории берутся из списка в src/components/Admin/ProjectForm.tsx.

-- Aktobe Steklo — строительство завода стеклотары и ж/д пути.
update projects
   set category = 'Промышленные объекты'
 where slug = 'aktobe-steklo-zavod-zhd'
   and category = 'Проектирование';

-- QazCement Industries — строительство ж/д подъездного пути.
update projects
   set category = 'Железнодорожная инфраструктура'
 where slug = 'qazcement-industries-kenkaly'
   and category = 'Проектирование';

-- ВНИМАНИЕ, статусы НЕ трогаем.
-- Брошюра помечает эти объекты «РЕАЛИЗОВАНО» — но это относится к ПРОЕКТНОЙ
-- документации (заключения госэкспертизы 2025–2026), а не к стройке.
-- В projects обе записи остаются в статусе planned/2026, потому что СМР
-- по ним не завершены. Отметку «РЕАЛИЗОВАНО» получают соответствующие
-- записи design_projects.
--
-- НЕ ТРОГАЕМ также projects.slug = 'keden-servis' (category «Проектирование»):
-- в записи нет ни описания состава работ, ни привязки к кейсу брошюры —
-- нечем подтвердить, СМР это или ПД. Требуется решение заказчика.
