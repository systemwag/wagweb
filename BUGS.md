# WAG — Баги и задачи на доработку

> **Как пользоваться:** Нашёл баг — добавь строку. Исправил — ставь `[x]` и пиши кто/когда.
> Формат: `- [ ] [Страница] Описание бага` + приоритет: 🔴 крит / 🟡 средний / 🟢 мелкий

---

## 🔴 Критические (блокируют функционал)

- [ ] [/contacts] Форма: Supabase не настроен — нужно прописать реальные ключи в `.env.local`
- [ ] [Все страницы] `NEXT_PUBLIC_SUPABASE_URL` = placeholder → данные берутся из seed, не из БД

---

## 🟡 Визуальные / UX баги

- [ ] [/] Hero: фоновое изображение (Unsplash) может не грузиться без интернета — нужен fallback цвет
- [ ] [/] Stats: анимация счётчика запускается при загрузке, а не при скролле на мобильных
- [ ] [/projects] Карточки: нет реальных изображений — placeholder с годом выглядит пусто
- [ ] [Все страницы] Footer: ссылки на соцсети (LinkedIn/Telegram/Instagram) ведут на `#` — **нужны реальные URL от заказчика** (логотип и «Политика конфиденциальности» уже починены)
- [ ] [/about] Карта Казахстана (SVG): города расставлены приблизительно, нужна точность
- [ ] [/contacts] Реквизиты: БИН/ИИК — тестовые данные, нужно заменить на реальные

---

## 🟢 Мелкие / Улучшения

- [x] [Header] Логотип: JSON-LD/OG ссылались на отсутствующий `/logo.png` → переведено на существующий `/icon.svg` (Claude, 2026-05-29). Опц. позже: добавить растровый `logo.png` для лучших OG-превью.
- [ ] [/] Страница `page.tsx`: главная использует `#anchor` в CTA-кнопках Footer — лучше `/contacts`
- [ ] [/projects/[id]] Описание проекта: второй абзац — заглушка, нужен реальный текст
- [x] [sitemap.ts] BASE_URL захардкожен → вынесен в `src/lib/site.ts` (`NEXT_PUBLIC_SITE_URL` + fallback), используется в sitemap/robots/layout (Claude, 2026-05-29)
- [x] [globals.css] Google Fonts: уже мигрировано на `next/font` (Space Grotesk/Onest/Outfit) — пункт неактуален
- [ ] [Все страницы] Нет `<meta name="viewport">` явно (Next.js добавляет автоматически, но стоит проверить)
- [x] [/privacy] Страница создана + ссылка в Footer ведёт на `/privacy` (Claude, 2026-05-29)

---

## ✅ Исправлено

- [x] [Build] Supabase browser-клиент крашил prerender — исправлено ленивой инициализацией (Claude)
- [x] [Header] Якорные ссылки `#anchor` заменены на реальные маршруты `/about`, `/services` и т.д. (Claude)

---

## 📋 Чеклист для ручного тестирования

### Функционал
- [ ] Открыть `localhost:3000` — главная загружается без ошибок в консоли
- [ ] Навигация: все пункты меню ведут на нужные страницы
- [ ] Header подсвечивает активную страницу
- [ ] Hamburger-меню работает на мобильном (≤900px)
- [ ] Фильтр на `/projects?category=...` фильтрует карточки
- [ ] Клик на карточку проекта → открывает `/projects/[slug]`
- [ ] `/contacts` → заполнить форму → проверить отправку (консоль / Supabase)
- [ ] `/sitemap.xml` открывается и содержит все маршруты
- [ ] `/robots.txt` отдаёт корректный файл

### Визуал
- [ ] Проверить на 375px (iPhone SE)
- [ ] Проверить на 768px (iPad)
- [ ] Проверить на 1440px (Desktop)
- [ ] Анимация Hero canvas работает
- [ ] Счётчики Stats анимируются при скролле
- [ ] Hover-эффекты на карточках работают
- [ ] Glassmorphism виден на карточках

### Производительность (Lighthouse)
- [ ] Performance score ≥ 85
- [ ] SEO score = 100
- [ ] Accessibility score ≥ 90
- [ ] Best Practices score ≥ 90
