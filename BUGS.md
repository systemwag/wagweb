# WAG — Баги и задачи на доработку

> **Как пользоваться:** Нашёл баг — добавь строку. Исправил — ставь `[x]` и пиши кто/когда.
> Формат: `- [ ] [Страница] Описание бага` + приоритет: 🔴 крит / 🟡 средний / 🟢 мелкий
>
> **Сверка с кодом: 2026-07-26.** Каждый открытый пункт проверен по репозиторию;
> то, что уже неверно, перенесено в «Закрыто» с пометкой почему.

---

## 🔴 Критические (блокируют функционал)

_Пусто._

---

## 🟡 Визуальные / UX баги

- [ ] [/projects, /geography] Координаты маркеров на карте Казахстана битые: у 11 из 32 записей
      `x_map`/`y_map` = NULL (объект вообще не виден), у записи id 21 «Уральск» `x_map = −9`,
      и три записи «Уральск» стоят в трёх разных точках страны. Чинится перетаскиванием
      в `/admin/map` (MapCalibrator) — данные, а не код. 🟡
- [ ] [/projects] Нет фотографий у 12 из 32 объектов — карточка показывает placeholder
      с годом. У обслуживания то же: 13 из 20 без фото. 🟡
- [ ] [/design] `year` не заполнен у 87 из 100 проектных работ, `location` — у 77.
      Сортировка и фильтр по годам поэтому бесполезны. Нужны первоисточники. 🟡
- [ ] [/contacts] ИИК и БИК в блоке «Реквизиты» не сверены со справкой банка
      (БИН 100340009758 — подтверждён, совпадает с `src/lib/company-facts.ts`). 🟢

---

## 🟢 Мелкие / Улучшения

- [ ] [/] Главная: CTA-кнопки Footer используют `#anchor` — лучше вести на `/contacts`. 🟢
- [ ] [Данные] `projects.length` работает не тем, чем называется: из 32 значений только два —
      реально длина («400 м», «9,504 км»), остальное — тип работ («Под ключ», «Демонтаж»,
      «Мачта связи»), 12 пустых. Разделить на `scope_label` + `length_m` либо переименовать. 🟢
- [ ] [Данные] `projects.date_start`/`date_end` пусты у 31 из 32 записей — либо заполнять,
      либо убрать колонки: год есть отдельным полем. 🟢
- [ ] [Данные] `design_projects`: два конкурирующих идентификатора — `number` ≠ `id`
      у 99 записей из 100. Маршрут `/design/[id]` ходит по `id`, а на карточке печатается
      `number`; `slug` вида `design-N` не используется нигде. 🟢
- [ ] [/design] Осталась одна пара одинаковых описаний: ТОО «Делдал», записи № 22 и № 76 —
      совпадают заказчик и состав работ, различить по данным БД нечем. 🟢
- [ ] [Deploy] `public/` весит >100 МБ, из них 57 МБ — три PDF в git
      (portfolio.pdf 25 МБ, portfolio-en.pdf 24 МБ, portfolio-design.pdf 8 МБ).
      Обсудить внешнее хранение. 🟢
- [ ] [/portfolio/print/design] `portfolio-design.pdf` устарел: кейсы влиты в основное
      портфолио. Пересобрать или удалить вместе с маршрутом. 🟢

---

## ✅ Закрыто

**Сверка 2026-07-26 — пункты были неактуальны:**

- [x] [/contacts] «Supabase не настроен, нужны реальные ключи» — настроен: проект
      `crmfrphqgxglumezbgae` слинкован, форма пишет в таблицу `contacts` через service role.
- [x] [Все страницы] «`NEXT_PUBLIC_SUPABASE_URL` = placeholder, данные из seed» — данные
      идут из БД: 32 СМР + 20 обслуживания + 100 проектных работ.
- [x] [/] «Hero: фон с Unsplash может не загрузиться» — Unsplash в проекте не используется,
      упоминаний в `src/` нет.
- [x] [Все страницы] «Footer: соцсети ведут на `#`, нужны URL от заказчика» — ссылок на
      соцсети в Footer нет вовсе, только email, телефон и внутренние страницы.
- [x] [/contacts] «БИН/ИИК — тестовые данные» — БИН реальный (100340009758), реквизиты
      Банка ЦентрКредит на месте. Осталась только сверка ИИК/БИК со справкой, см. выше.
- [x] [/projects/[id]] «Второй абзац описания — заглушка» — заглушек в коде нет,
      описание берётся из БД.
- [x] [Все страницы] «Нет явного `<meta name="viewport">`» — Next.js добавляет сам,
      отдельная настройка не нужна.
- [x] [/] «Stats: счётчик стартует при загрузке, а не при скролле» — анимация привязана
      к появлению блока (`phase !== 'idle'` в KazakhstanMap/Stats).

**Ранее:**

- [x] [Header] Логотип: JSON-LD/OG ссылались на отсутствующий `/logo.png` → переведено на
      `/icon.svg` (Claude, 2026-05-29).
- [x] [sitemap.ts] BASE_URL захардкожен → вынесен в `src/lib/site.ts` (Claude, 2026-05-29).
- [x] [globals.css] Google Fonts → `next/font` (Space Grotesk / Onest / Outfit).
- [x] [/privacy] Страница создана, ссылка в Footer работает (Claude, 2026-05-29).
- [x] [Build] Supabase browser-клиент крашил prerender → ленивая инициализация (Claude).
- [x] [Header] Якорные `#anchor` заменены на реальные маршруты (Claude).
- [x] [/projects, /design] 3D-наклон карточек за курсором мешал попадать по ссылкам —
      убран, заменён обычным hover (Claude, 2026-07-26).
- [x] [/projects/[id]] Галерея не переключалась: миниатюры рисовались `<div>` без
      обработчика клика → вынесена в клиентский `ProjectGallery` (Claude, 2026-07-26).
- [x] [Безопасность] На таблице `projects` висели политики INSERT/UPDATE/DELETE для роли
      PUBLIC с `using(true)` — анонимный ключ давал право менять реестр. Сняты
      (миграция `20260726062414_harden_rls_policies`, Claude, 2026-07-26).

---

## 📋 Чеклист для ручного тестирования

### Функционал
- [ ] Открыть главную — загружается без ошибок в консоли
- [ ] Навигация: все пункты меню ведут на нужные страницы, активная подсвечена
- [ ] Hamburger-меню работает на мобильном (≤900px)
- [ ] Фильтр на `/projects?category=...` фильтрует карточки
- [ ] `/design`: карточки с чертежами наверху, поиск и вкладки фильтруют и их тоже
- [ ] `/projects/[slug]`: галерея переключается кликом и стрелками ←/→
- [ ] `/contacts` → форма → заявка доходит до `/admin/contacts` и в Telegram
- [ ] `/sitemap.xml` содержит публичные маршруты и НЕ содержит `/effects`,
      `/portfolio/print*`, `/construction-3d*`
- [ ] `/robots.txt` закрывает `/admin`, `/api`, `/effects`, `/portfolio/print`, `/construction-3d`

### Визуал
- [ ] 375px (iPhone SE), 768px (iPad), 1440px (Desktop)
- [ ] Hover-эффекты на карточках, glassmorphism
- [ ] Счётчики Stats анимируются при появлении блока

### Производительность (Lighthouse)
- [ ] Performance ≥ 85 · SEO = 100 · Accessibility ≥ 90 · Best Practices ≥ 90
